aggregate ^(test\.[^.]+\.)
    every 10 seconds
    expire after 30 seconds
    timestamp at start of bucket
    compute percentile95 write to \1p95
    send to blackhole;

match * send to blackhole;
